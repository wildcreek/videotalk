<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mateo.videotalk.dao.UserDao">
    <!--为DAO接口方法提供sq语句配置-->
    <select id="getLatestUserIDByClientType" >
        SELECT
        userID
        FROM
        useraccount
        WHERE
        clientType = #{clientType}
        ORDER BY
        userID
        DESC
        LIMIT
        0,1
    </select>
    <select id="findUserByUserAccount" resultType="User">
        SELECT
          userAccount,clientType,clientID,loginType,phoneNumber,province
        FROM
          useraccount
        WHERE
          useracount = #{userAccount}

    </select>
    <insert id="insertUser" >

        INSERT INTO
        useraccount ( userID,userAccount,clientType,clientID,loginType,phoneNumber,province)
        VALUES
         (#{userID},#{userAccount},#{clientType},#{clientID},#{loginType},#{phoneNumber},#{province})

    </insert>
    <update id="updateUser" >
        UPDATE
        useraccount
        SET
        clientID = #{clientID}
        WHERE
        useracount = #{userAccount}

    </update>
</mapper>